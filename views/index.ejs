<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <header>
    <div class="container">
      <h1 class="logo">Фанаты и точка</h1>
      <nav>
        <ul>
          <li><a href="/">Главная</a></li>
          <li><a href="/explore">Изучить</a></li>
          <li><a href="/messages">Мессенджер</a></li>
          <% if (!user) { %> <!-- Если пользователь не авторизован -->
            <li><a href="/login">Войти</a></li>
            <li><a href="/register">Регистрация</a></li>
          <% } else { %> <!-- Если пользователь авторизован -->
            <li><a href="/profile">Профиль</a></li>
            <li><a href="/logout">Выйти</a></li>
          <% } %>
        </ul>
      </nav>
    </div>
  </header>

  <main>
    <div class="container">
      <section class="hero">
        <h2>Фанаты и точка</h2>
        <p>Следите и подписывайтесь на ваших любимых авторов</p>
        <a href="#content" class="explore-btn">Изучить</a>
      </section>
      
      <section class="search">
        <input type="text" id="search" placeholder="Поиск авторов..." oninput="filterAuthors()">
      </section>
      <section class="content" id="content">
        <div class="authors-grid">
          <% authors.forEach(author => { %>
            <div class="author-card">
              <a href="/profile/<%= author.id %>" onclick="console.log('Переход на профиль:', <%= author.id %>);"> <!-- Ссылка на профиль автора -->
                <!-- Ссылка на профиль автора -->
                <img src="<%= author.profilePicture ? author.profilePicture.replace('public/', '') : '/uploads/default-avatar.png' %>" alt="<%= author.profileName || 'Имя отсутствует' %>" class="author-avatar">
                <h3><%= author.profileName %></h3>
                <p><%= author.Description || 'Описание отсутствует' %></p>
              </a>
              <form action="/subscribe/<%= author.id %>" method="POST" style="margin-top: 10px;">
                <button type="submit">Подписаться</button>
              </form>
            </div>
          <% }) %>
        </div>
      </section>
      
      <script>
        document.querySelectorAll('.profile-link').forEach(link => {
          link.addEventListener('click', function(event) {
            event.preventDefault(); // Предотвращаем переход по ссылке
            const profileUrl = this.href; // Сохраняем ссылку
            console.log(`Пользователь нажал на ссылку профиля: ${profileUrl}`); // Логирование в консоль
      
            // Переход по ссылке после логирования
            window.location.href = profileUrl;
          });
        });
      </script>
      
      
     
    </div>
  </main>

  <footer>
    <div class="container">
      <p>&copy; 2024 Фанаты и точка. Все права защищены.</p>
    </div>
  </footer>
  <script src="/scripts/index.js"></script>
  <script>
    function filterAuthors() {
      const input = document.getElementById('search').value.toLowerCase();
      const cards = document.querySelectorAll('.card');

      cards.forEach(card => {
        const name = card.querySelector('h3').textContent.toLowerCase();
        if (name.includes(input)) {
          card.style.display = '';
        } else {
          card.style.display = 'none';
        }
      });
    }
  </script>
</body>
</html>
