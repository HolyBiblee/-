<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <header>
    <div class="container">
      <h1 class="logo">Фанаты и точка</h1>
      <nav>
        <ul>
          <li><a href="/">Главная</a></li>
          <li><a href="/explore">Изучить</a></li>
          <li><a href="/messages">Мессенджер</a></li>
          <% if (!user) { %>
            <li><a href="/login">Войти</a></li>
            <li><a href="/register">Регистрация</a></li>
          <% } else { %>
            <li><a href="/profile">Профиль</a></li>
            <li><a href="/logout">Выйти</a></li>
          <% } %>
        </ul>
      </nav>
    </div>
  </header>

  <main>
    <div class="container">
      <section class="hero">
        <h2>Фанаты и точка</h2>
        <p>Следите и подписывайтесь на ваших любимых авторов</p>
        <a href="#content" class="explore-btn">Изучить</a>
      </section>

      <section class="search">
        <input type="text" id="search" placeholder="Поиск авторов..." oninput="filterAuthors()">
      </section>

      <section class="content" id="content">
        <div class="authors-grid">
            <% authors.forEach(author => { %>
                <div class="author-card" data-id="<%= author.id %>">
                    <script>
                        console.log('Author ID:', <%= author.id %>); // Логируем ID автора
                    </script>
                    <a href="#" onclick="goToProfile(<%= author.id ? author.id : 'null' %>); return false;">Профиль</a>
                    <img src="<%= author.profilePicture ? author.profilePicture.replace('public/', '') : '/uploads/default-avatar.png' %>"
                         alt="<%= author.profileName || 'Имя отсутствует' %>"
                         class="author-avatar">
                    <h3><%= author.profileName %></h3>
                    <p><%= author.Description || 'Описание отсутствует' %></p>
                    
                    <form action="/subscribe/<%= author.id %>" method="POST" style="margin-top: 10px;">
                        <button type="submit">Подписаться</button>
                    </form>
                </div>
            <% }) %>
        </div>
    </section>
     

      <script>
        function goToProfile(authorId) {
            console.log('Navigating to profile with ID:', authorId); // Логируем переход на профиль
            if (authorId) {
                window.location.href = `/profile/${authorId}`; // Переход на страницу профиля
            } else {
                console.error('Author ID is null or undefined'); // Логируем ошибку, если ID отсутствует
            }
        }

      

        function filterAuthors() {
          const input = document.getElementById('search').value.toLowerCase();
          const cards = document.querySelectorAll('.author-card'); // Исправьте селектор, чтобы соответствовать классу

          cards.forEach(card => {
            const name = card.querySelector('h3').textContent.toLowerCase();
            if (name.includes(input)) {
              card.style.display = '';
            } else {
              card.style.display = 'none';
            }
          });
        }
      </script>
    </div>
  </main>

  <footer>
    <div class="container">
      <p>&copy; 2024 Фанаты и точка. Все права защищены.</p>
    </div>
  </footer>
</body>
</html>
